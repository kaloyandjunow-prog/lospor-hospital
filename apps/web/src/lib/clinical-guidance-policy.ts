/**
 * Preserve the established medication-entry surface by default: configured
 * ranges, quick values, sliders and +/- controls remain visible. The separate
 * appliance guidance policy may still disable calculated assistance when a
 * Status administrator deliberately turns it off.
 */
export const DISPLAY_CLINICAL_DOSE_GUIDANCE = true
