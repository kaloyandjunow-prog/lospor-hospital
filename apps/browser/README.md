# LOSPOR Hospital Research Browser

The local research, audit, quality, and benchmarking interface. It never
connects directly to PostgreSQL and receives governed data only through the
Hospital API.

The reference appliance exposes this service only to configured Hospital
VPN/LAN ranges. Application permissions remain mandatory inside that network
boundary.

Use the repository root installation and release documentation.
